theme = "base16"

[editor]
bufferline = "multiple"
idle-timeout = 0
# [editor.whitespace]
# render = "all"
[editor.soft-wrap]
enable = true

[keys.normal]
K = "half_page_up"
J = "half_page_down"
"," = ["goto_prev_diag", "code_action"]

[keys.select]
K = "half_page_up"
J = "half_page_down"
